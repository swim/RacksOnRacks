/**
 * @file
 * defaults.js
 */

var defaults = {
  // Auto calculates if not set.
  perRow: 0,
  // Element pattern width/ height.
  element: {
    width: 32,
    height: 64
  }
};

module.exports = defaults;